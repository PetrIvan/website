@AGENTS.md

## Claude Code

Treat `AGENTS.md` as the canonical shared project instructions. Keep this file
and the wrappers in `.claude/skills/` minimal and compatibility-only. Mirror
each canonical skill from `.agents/skills/` with a wrapper of the same name.
